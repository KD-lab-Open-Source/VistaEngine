# Renderer porting register — what D3D9 did that SDL GPU does not yet

The D3D9 backend is gone; SDL GPU is the renderer on every platform. Most of the engine's
rendering came across, but the features below did **not**: they existed only in the D3D9
branches, and those branches were deleted rather than carried as dead code.

Nothing here is lost. **`ca9aa43` is the last commit with the D3D9 implementations intact** —
read the original alongside the port:

```sh
git show ca9aa43:Render/src/Grass.cpp          # the D3D9 draw path for any file below
git show ca9aa43 --stat                        # the tree as it was
```

The original HLSL/asm shader sources are still in the repo under `Render/shader/**/*.vsl`,
`*.psl` — they were kept deliberately. **Read them before reimplementing an effect.**

Every site is marked in the source with a greppable tag:

```sh
grep -rn "TODO(sdl-port)" --include=*.cpp --include=*.h .
```

---

## Ported since the retirement

| # | Feature | Now lives in |
|---|---------|--------------|
| 1 | **Grass** | `Render/SDLGrassRenderer.{h,cpp}` + `Render/SDLShaders/grass.{vert,frag}.hlsl`, driven from `GrassMap::Draw`/`DrawGrass` as the original drove `VSGrass`/`PSGrass`. Everything but the draw (tile grid, blade generation, sort, buffers) had always been portable. The alpha test (`D3DRS_ALPHAREF 100`) has no SDL GPU equivalent and became a `clip()` in the fragment shader. |
| 2 | **Distance fog** | `cSDLRenderDevice::SetGlobalFog` + `fogPlane()`, and a fog term at the end of every world fragment shader: terrain, objects, grass, water, and the world quads (particles, coast foam, wave sources, light columns). Driven from `Environment::graphQuant`, as before. The world-quad renderer has two rules, since its groups are not all occluders: additive/subtractive ones fade to nothing (the original's `FIX_FOG_ADD_BLEND`), the rest fade toward the fog colour. Three cameras keep fog OFF and must stay that way — the sky (`cSkyObj::Draw`), the lightmap (`CameraPlanarLight::DrawScene`) and the 2D pass (`Camera::Set2DRenderState`). The under-water post-effect's fog override, once blocked on #6, is back too (#6a). |
| 3 | **The perimeter field dome** | No renderer of its own: it is a `SetWorldMaterial` caller with `sVertexXYZDT2` geometry, so it goes through **`SDLWorldQuadRenderer`'s triangle route**, which already had the blend, the two textures and the `COLOR_ADD` operation. Added there: `DrawIndexedPrimitive` (the dome's tile grid shares vertices, so neither a strip nor a list expresses it) and the `ZREFLECTION` variant in `worldtri.{vert,frag}.hlsl` — the height clip against the water's A8L8 map that stops the dome showing through the hills. Also restored `tilemap_inv_size`, a D3D-only global `cTileMap`'s constructor sets. |
| 5 | **Cloud shadows** | `Render/SDLCloudShadowRenderer.{h,cpp}` + `Render/SDLShaders/cloudshadow.{vert,frag}.hlsl`, driven from `cCloudShadow::Draw`. Note this is a **lightmap** effect, not a view one: `cCloudShadow` carries `ATTRCAMERA_SHADOW`, which is the planar *light* camera's attribute, so its world-sized quad overwrites the terrain lightmap (`ALPHA_NONE`, `sortIndex -1`) before `drawLights()` blends over it. The shader is centred on 0.5 — the lightmap's neutral — so the terrain and the grass pick the clouds up through the lightmap term they already sample. Neither of their shaders changed. |
| 6a | **Post effects: monochrome + under-water** (the reachable half of the stack — pause and the campaign triggers use monochrome; the under-water effect is on by default and carries the fog override) | `Render/SDLPostEffectRenderer.{h,cpp}` + `Render/SDLShaders/{posteffect.vert,copy.frag,monochrome.frag,underwater.frag}.hlsl`, recorded from the (still live) `PostEffectManager` effects in `VistaRender/postEffects.cpp` and composited by `cSDLRenderDevice::drawPostEffects`. **The frame is turned around**: D3D9 `StretchRect`ed the back buffer per effect, but SDL GPU cannot sample the swapchain — so when `Environment::graphQuant` knows an effect will draw (`PostEffectManager::anyEffectWillDraw`), it arms a scene-capture target (`armSceneCapture`) that stands in for the screen for the whole scene walk, and the chain composites capture → swapchain before the UI pass. Zero cost on the (nearly all) frames with no active effect. The under-water fog override (`PostEffectUnderWater::setFog`) is back in `graphQuant`, closing the gap left by #2. |
| 6b | **Lens flare** (`isEnabled` in 26 of 28 shipped worlds, daytime) | `Render/src/LensFlare.cpp` draws again: the 2D sprite chain through the UI batch (`DrawSprite`, which now honours `ALPHA_ADDBLENDALPHA` — `SDLUIRenderer` grew a per-run blend), the world-space glow billboard through `SDLWorldQuadRenderer`'s quad route. Two divergences, both deliberate: SDL GPU has **no occlusion queries** (#20), so the D3D 25-point ring test became a CPU test — sun on screen and in front of the camera, edge-faded — which gives up occlusion by terrain/objects; and the sprites ride the UI batch, which replays *after* the post-effect composite, so monochrome does not grey the flare as it did on D3D9. |
| 7 | **`cEnvironmentEarth`** (the ground plane) **+ the water border skirt** | `Water/Water.cpp` + `Render/SDLEnvironmentEarthRenderer.{h,cpp}` + `Render/SDLShaders/environmentearth.{vert,frag}.hlsl`. The terrain-coloured plane that fills the world beyond the map edge, under the horizon fog ring (#8) — the *ground* half of the map-edge gap #8 left open. Its vertex/index buffers were always portable and always built; only the draw (`psEnvironmentEarth`) was missing. Unlike the fog ring, it is an **opaque occluder, not a blended overlay**, so it gets its own renderer rather than riding `SDLWorldQuadRenderer` (which never writes depth): it draws at `SCENENODE_OBJECTFIRST`, which `Camera::DrawScene` reaches **before** the terrain, so the pass takes the depth clear, writes the plane's depth, and the terrain sorts over it. `ALPHA_NONE`, depth test + write on, as the original's `SetWorldMaterial(ALPHA_NONE)` asked. The fragment shader is a verbatim port of `EnvironmentEarth.psl`: `ot.rgb = texture.rgb * tfactor * 2`, where `tfactor` is the tile map's diffuse folded with the sun angle (the same terrain tint the map lights with, so the ground beyond the edge matches the ground inside it and darkens together at dusk) times the object colour — built in `cEnvironmentEarth::Draw` exactly as it built the D3D constant; the `*2` is the fixed-function `x2` modulate, carried at full precision rather than folded into an 8-bit vertex colour. The reflection camera is skipped (the plane sits at water height and would fight the surface in the mirror). **Landing with it: the water border** (`cWater::InitBorder`/`Border::Draw`) — eight tiles extending the sea from the map edge out to the horizon — was gated behind `gb_RenderDevice3D` (null off-Windows), so on water worlds the sea stopped at the map edge and `pEnvironmentEarth` was never even created; its buffers were portable too, so it now builds through `gb_RenderDevice` and rides `SDLWaterRenderer`'s own pass, giving the skirt the reflection, sun glint and depth-opacity the map's water has. |
| 8 | **The horizon fog ring** (`cFogCircleEX`, on by default — `fog_enable_` defaults true) | `Water/SkyObject.cpp`. Like the field dome (#3), it is a per-vertex `SetNoMaterial` caller, so it rides **`SDLWorldQuadRenderer`'s triangle route** with no renderer of its own: a cylinder wall around the map (radius 2.5× the map size, an opaque band at the horizon fading to clear above, plus a `FOG_CENTER` triangle floor), drawn from `cSkyObj::DrawSky` after the sky models and **while fog is still disabled**, so it lands over the sky and behind the world — the backdrop that closes the sky→sea seam at the map edge. The original shaded RGB from `D3DRS_TEXTUREFACTOR` (the current fog colour) through a fixed-function `SELECTARG1` stage and took alpha from the vertex diffuse; that TFACTOR stage has no SDL equivalent, so the fog colour is **baked into the vertex diffuse RGB** instead — with the white 1×1 stand-in on texture 0, `worldtri` outputs `texture*diffuse` = the premultiplied fog colour, which `ALPHA_BLEND` lays down exactly as the ring did (no shader change). It re-records every frame (the route clears each frame), so `cFogCircleEX` keeps no device buffers; only the index pattern is built once. The **ground-plane** half of the map-edge gap is #7 (`cEnvironmentEarth`), ported alongside this. |
| 10 | **Fog of war** | Another **lightmap** effect, and the reason that map has an alpha channel at all. The lightmap packs two unrelated things: the light sources live in RGB, the fog of war in **alpha**. `FogOfWar::Draw` writes the alpha with one world-sized `ALPHA_NONE` quad through `SDLWorldQuadRenderer`'s quad route (no renderer of its own), masked to alpha with the new `SetColorWriteMask` (#16); `CameraPlanarLight::drawLights` now masks the other way, to RGB, so the light quads cannot punch holes of clear visibility through the fog. The consumer is one line restored at the end of `tilemap.frag.hlsl` and `grass.frag.hlsl` — `ot.rgb = lerp(ot.rgb, vFogOfWar, lightmap.a)`, after the shadow and before the distance fog, where the originals put it — gated on `LightMapParams.y` and fed from `cSDLRenderDevice::SetFogOfWar`, which `cScene::Draw` drives as it drove `cD3DRender::SetFogOfWar`. The coverage map (`FogOfWar::UpdateTexture`, the sight circles, `FogOfWarMap`) was always portable and always running. **Caveat, and it is a big one: no shipped world switches this on.** `is_fog_of_war` is gated out of `MissionDescription::serialize` by `GlobalAttributes::enableFogOfWar`, which was false when these worlds were saved, so the key is absent from every `.spg` and the text archive leaves it at `false` — and `Universe.cpp`'s `if(environment->fogOfWar())` gates the *simulation* on the same object. Fog of war is therefore off in all of retail P2, on D3D9 as much as here. This restores the capability, not a visible fix; to see it, set `is_fog_of_war` on a world (the editor exposes it). Verified by forcing that flag: shroud, sight circles, and grass darkening with the ground under it. |
| 10b | **`FieldOfViewMap`** | The fog of war's sibling: the *other* quad into the terrain lightmap, and the reason the collision in #10 was worth spelling out. The sight sectors live in the lightmap's **RGB** (a per-player colour tint), the fog of war in its **alpha**. `FieldOfViewMap::Draw` ports the original `ALPHA_BLEND` quad through `SDLWorldQuadRenderer`'s quad route, but masked to **RGB** with `SetColorWriteMask` (#16) — the same recipe `CameraPlanarLight::drawLights` uses. That resolves the collision the D3D9 version left implicit: its alpha (`128 + (color.a*visibility >> 9)`, see `updateTexture`) would otherwise land in the channel the fog of war now owns and read as coverage. Masking it away costs nothing — `FogOfWar::Draw` (sortIndex 10) overwrote that alpha after us (sortIndex 0) when fog was on, and nothing samples it when fog is off, so the alpha write was never meaningfully consumed even on D3D9. The tint is packed centred on 128 = 0.5 = the lightmap's neutral, so untraced ground contributes nothing and the terrain/grass pick a traced cell up through the `light += 2*(lightmap.rgb - 0.5)` term they already sample — no shader change. `updateTexture()` is rewired at the end of `Environment::graphQuant` where the original drove it, reading `sdlRenderDevice()->planarTransform()` (the D3D `gb_RenderDevice3D` is null off-Windows). **Same caveat as #10: no shipped content switches it on.** `showSightSector` defaults `false` on the unit attribute and no `.spg`/attribute sets it, so `IronLegion`/`IronBuilding` never call `add()` — field of view is off in all of retail P2, on D3D9 as much as here. Verified by forcing the flag: per-unit sight discs tinted into the ground, decaying as units move, under the normal lighting. |
| 9 | **Sky cubemap**, and the reflection materials that read it | `Render/src/RenderCubemap.cpp` (`cRenderSky`) draws again, from `Environment::graphQuant` via `EnvironmentTime::Draw`. All six faces on the first frame, then **one face per frame**, and only the *sky* scene goes into each — about one extra sky render a frame. **The faces are not rendered into directly**: every renderer here builds its own `SDL_GPUColorTargetInfo`, so aiming a pass at one cube layer would mean a layer argument through all ten of them; instead a face is drawn into an ordinary offscreen 2D target (the path planar water reflection already proves) and `cSDLRenderDevice::copyToCubeFace` moves it into the layer — one 256×256 copy per frame. `createCubeTexture` pre-fills all six faces with the sky fone colour, because a face is only written when a pass opens on it and the first sky draw of a world records nothing, which otherwise sampled as magenta for the first frames. **`copyToCubeFace` must record into the frame's command buffer, not one of its own**: `flushTarget` only *records* the face's render pass into `commandBuffer_`, which is not submitted until `EndScene`, so a separately acquired buffer submitted on the spot runs *first* and copies `pFaceTarget` before anything has been drawn into it. That put the previous face into every slot — the whole cube rotated by one — so every lookup returned a neighbour, and all six faces took uninitialised magenta on the first pass. It is invisible in a still sky (the faces differ little) and shows up as a reflection that is plausible but wrong: the water asked for the sky overhead and got the horizon band. The consumer is `mat.is_reflect_sky` (a material whose reflect map is named `sky.*`) and the shader is a **permutation of the reflection shader we already had** — the original used one `vsSkinReflection`/`psSkinReflection` with an `is_cube` flag off the bound texture's `TEXTURE_CUBEMAP`, so `REFLECT_CUBE=1` adds the original's `dir + (2*dot(n,dir))*n` world reflection vector and a `TextureCube` sample. Also removed the `!mat.is_reflect_sky` exclusions in `cObject3dx::Draw`, which had kept these materials out of the reflection *and* bump branches — the original tests `pReflectTexture \|\| is_reflect_sky` together. Its **other** consumer is the water: `WATER_REFLECTION` (row 25) samples this cube whenever the reflection option is off. **The old note that this had "no consumer" was true of Perimeter 2 only**: 116 of Maelstrom's 1031 models set it, including the city buildings, whose glass diffuse averages RGB(45,42,31) and renders black without the reflection. |
| 12 | **Float Z-buffer camera → the scene-depth snapshot** (its one live consumer: the soft-particle fade — `OPTION_SOFT_SMOKE` drives the emitters' `softSmoke` key, and the coast sprites always asked for it) | The original existed because D3D9 could not sample its own depth buffer: a child camera re-rendered terrain/objects/grass depth into a float texture (`ZBuffer/*.vsl` store pre-divide clip z), and `standart.psl`'s `FLOAT_ZBUFFER` faded a billboard by `saturate((zb − zpos.z) * 0.1)`. SDL GPU samples depth directly, so `cSDLRenderDevice::snapshotSceneDepth` instead **copies the frame's depth buffer once per frame** at the opaque→transparent boundary (first soft group in `drawWorldQuads` pays; the water reflection and other offscreen targets draw with the fade off), and `worldquad.frag.hlsl` converts both the sample and the fragment's own hardware depth back into **the original's clip-z units** through the camera's projection constants (`clipZOf`; note `Camera::Update` leaves `matProj._44` at 1, so all four constants matter) — the `0.1` falloff carries over verbatim. No second scene walk, no depth-output shader variants. One divergence: the snapshot holds the whole opaque scene, so option value 1 ("terrain only") behaves as 2 ("objects and terrain"). The other consumer of the float map, DOF, is still #6. |
| 4 | **Lava + ice terrain materials, and the ice sheet on water** | Three consumers, two shader pairs. **Lava terrain** and **ice terrain** are the placement-zone materials `cTileMap::setMaterial` switched to `lavaShader_` / `iceShader_` for; both fold into `SDLTileMapRenderer`, drawn as their own material runs over the terrain mesh **in the same pass** (a separate pass would z-fight the cells) with `Render/SDLShaders/tilemap_lava.{vert,frag}.hlsl` / `tilemap_ice.{vert,frag}.hlsl`. Lava is a four-octave value-noise fBm through a 64³ `graphRnd()` volume (`CreateVolumeRand`) scrolled in Z by time, floored by a ground texture, emissive (reads no sun, no shadow map). Ice is the snow texture tinted by `cScene::GetPlainLitColor` over a bump-perturbed **planar reflection** — the water's own reflection camera/target and `fillMirrorMatrix` — falling back to plain snow where there is no reflection camera (the sky-cubemap fallback, #9, now exists but is not wired to this consumer). The **ice sheet on water** (`cTemperature::Draw`) draws the same water polygons a second time through **`SDLWaterRenderer`'s new ice path**: a second recording list (`SetIceState`/`DrawIce`, driven by `cSDLRenderDevice::drawWaterIce`) so it lands in its own alpha-blended, depth-read-only pass over the surface, keyed off the temperature coverage grid (`water_ice.{vert,frag}.hlsl`, the `USE_ALPHA` path). The 8-bit L8 coverage widens to `(255,255,255,coverage)` off-Windows, so the shader reads coverage from `.a`, and the D3D9 `ALPHAREF` test became a `clip()` (as grass #1 did). One fix outside the renderer: `cWater::CalcColor` left the vertex **red** uninitialized — the water shaders only ever read the alpha, but the ice's `aa = diffuse.r` coverage weight read it too (undefined on D3D9, working by luck); set to white so the coverage is well-defined and the shallow-cutoff still drops ice off open land. **Dead in retail, like #10/#10b: no shipped world paints a placement-zone material, sets `isTemperature`, or `anywhereIce`.** A capability port, verified by forcing each (a painted centre patch; a flooded coverage grid): animated red lava, snowy/reflective ice terrain, and a reflective ice sheet over the water. |
| 19 | **Two-pass Z prepass for fading objects** | `cSimply3dx::Draw`, and a `TwoPass` mode on `SDLObject3dxRenderer`. `IsDraw2Pass()` is a decor object mid-fade whose texture carries **no** opacity channel — so the fade is uniform over the whole model, and drawn once with `ALPHA_BLEND` every overlapping layer blends in turn and the object shows its own far side through its near one. The mesh is recorded twice: `PASS_DEPTH_ONLY` fills depth with the colour write mask at 0, then `PASS_DEPTH_EQUAL` shades with depth write off and `COMPAREOP_EQUAL`, so each pixel blends exactly once. The original spread this across `D3DRS_ZWRITEENABLE`/`COLORWRITEENABLE`/`ZFUNC` around two `DrawModels` calls; here the state rides the pipeline, so it is folded into `pipelineFor`'s key (2 bits) and the pair is bracketed with `SetTwoPass`. Depth-EQUAL is exact because both halves run the **same** vertex shader over the same geometry. Both halves also drop culling, as the original's `RS_CULLMODE -1` did. The alpha-test reference is **not** carried and does not need to be: D3D9 enabled the test for `ALPHA_BLEND` too (`D3DRender.cpp:1777`), and `IsDraw2Pass` scaled the ref by the fade precisely so that `alpha >= 80/255*alpha` held for every texel — the trick's whole effect was to stop the test culling the model as it faded, and here nothing clips outside `ALPHA_TEST`. Reachability is not in doubt: `Simply3dx.cpp:56` sets the attribute whenever `diffuse.a*opacity*distance_alpha < AlphaMaxiumBlend`, i.e. on every distance-faded decor object in shipped content. |
| 22 | **Unit silhouettes** — the coloured outline a unit shows through a building it walks behind | `Camera::DrawSilhouetteObject` + a silhouette mode on `SDLObject3dxRenderer` + the `SILHOUETTE=1` permutation of `object3dx.frag.hlsl`. Each object is drawn twice — `PreDraw` routes them here *instead of* `SCENENODE_OBJECT`, so this function also owes them their ordinary draw — and the outline is one flat colour, no texture, no light, no fog, no blend. **Not through the stencil buffer, and deliberately so.** D3D9 stamped `STENCILZFAIL=REPLACE` — "where the depth test failed" — and then tinted those stencil values with a screen-space box per object (`DrawSilhouettePlane`, which now returns early: SDL GPU has no stencil here and needs none). Drawing the object itself with `COMPAREOP_GREATER` selects the same fragments with no stencil at all, and that matters: the scene depth buffer is `D32_FLOAT`, and giving it stencil would drag `sceneDepthCopy_` — the soft-particle depth source, #12 — onto a format next to which only `D16_UNORM` is universally sampleable, to buy a capability nothing else in the register asks for. **Two things the depth route needs that the stencil route did not, both found by testing, both non-obvious:** *(1) The outline must be drawn BEFORE these objects write any depth of their own* — i.e. before `DrawObject(SCENENODE_FLAT_SILHOUETTE)`, against a depth buffer holding the terrain and the buildings and none of the units being outlined. Drawn after, a unit's own nearer parts (a raised arm over its chest, a turret over its hull) have already written depth, its farther parts test `GREATER` against them and pass, and the outline paints across the middle of a unit standing in plain sight. The original was immune only because the occlusion query gated it *object-wide*: a visible object set `STENCILZFAIL` to `KEEP` and stamped nothing anywhere. That gate is not decoration — per-fragment depth is only safe when the object is excluded from the depth it is tested against. *(2) The outline pipeline must keep the SAME vertex shader as the object's plain draw.* `pipelineFor` folds `bump`/`reflect`/`secondOpacity` out of the caster's key; doing the same here selects a different VS, and two separately compiled shaders computing the same position may disagree in the last bit (one contracts a multiply-add into an fma, the other does not). `GREATER` then stops meaning "behind something else" and starts meaning "rounded the other way". Maelstrom's silhouetted units are all `bump=1`, so this was live, not theoretical. The outline's fragment shader declares position alone, which is what lets it pair with every VS permutation. `ATTR3DX_ALWAYS_FLAT_SILUETTE` becomes `COMPAREOP_ALWAYS` and runs in a second loop *after* the plain draw (the original's other branch, `STENCILPASS` and `STENCILZFAIL` both `REPLACE`); before it, the object would paint back over its own tint. Untested — no unit in the shipped data sets the attribute. Back faces stay culled; `RS_CULLMODE -1` asks for the camera's cull, it does not turn culling off. Child cameras draw the objects and no outline, as the original's `if(Parent)` did. The colour rides in the existing `Diffuse` uniform, so the Material cbuffer's layout is untouched. **Two divergences.** The object-wide 300 ms hysteresis is gone with the occlusion query (#20): occlusion is judged per fragment and instantly, so a partly hidden unit outlines only its hidden part. And the original tinted last, after everything; this paints before the transparent and particle passes, which can therefore draw over a silhouette instead of under it. Also note `cVisGeneric::EnableSilhouettes` used to gate on `gb_RenderDevice3D->IsPS20()` — a D3D9 capability check against a permanently null pointer, so the flag was pinned false and the whole effect was dead however the mission was authored (see the `gb_RenderDevice3D` note below). **Unreached on retail Perimeter 2** (its `drawList` never fills); Maelstrom's data uses it through `showSilhouette`. See Maelstrom-PORTING.md. |
| 23 | **The KD-lab logo splash** (`cBlobs`) — the fish swimming under a screen full of metaballs, fired by `ActionShowLogoReel` from the global trigger chain | `Render/SDLBlobsRenderer.{h,cpp}` + `Render/SDLShaders/blobs.frag.hlsl`, driven from `UserInterface/Bubles/Blobs.cpp` and composited by `cSDLRenderDevice::drawBlobs`. Two passes, which is what `cBlobs` always was: the cells accumulate **additively** into an offscreen field (`ALPHA_ADDBLEND`, one falloff sprite per `kdCell`, so overlapping cells sum and merge into one shape — the metaball trick), then one fullscreen triangle reads that field and the frame and writes the result. Only the composite needed a new shader (a port of `Render/shader/PostProcessing/blobs.psl`'s `TYPE==3`, the only branch the file compiled): the cell pass is a pixel-space quad shaded `texture * diffuse`, which is exactly `ui.{vert,frag}.hlsl`, so it reuses them unchanged and only the blend differs. **The frame it refracts is the scene capture** (#6a): D3D9 `StretchRect`ed the back buffer into a texture each frame, and SDL GPU cannot sample the swapchain, so `cBlobs::BeginFrame` arms the capture *before* the scene walk and `drawBlobs` stands in for `drawPostEffects` — same settle, different composite. The splash has no `Environment`, hence no `PostEffectManager`, so the two never collide. Two things in `blobs.psl` read oddly and are ported verbatim: `mul ? out1 : out2` is a comparison against **zero**, not a lerp (every pixel the field touches at all takes the tinted, displaced path; the soft `smoothstep` is spent on the highlight and the surrounding 80% darkening), and the refraction offset is a **scalar** added to both UV components. The `IsPS20()` fallback — cells drawn straight to the back buffer in `cBlobsSetting::color_` — is not ported; it was for cards below PS 2.0. **A dependency outside the renderer:** the splash is interactive (cells flee the cursor, the fish follows it) and reads it by *polling* `GetCursorPos`, which off-Windows was a stub returning (0,0) — the same class of gap as `GetAsyncKeyState`. It is now latched by the SDL pump (`PlatformSetMousePosition`), window-relative, since the shim's `ScreenToClient` is the identity. The modal loop also moved off `PeekMessage` onto `pumpApplicationEvents`, as `showModal` already had. |
| 25 | **`WATER_REFLECTION` — the sky-cubemap water technique** | `Render/SDLShaders/water.{vert,frag}.hlsl` built a third way (`CUBE=1`, from `Render/shader/Water/water_cube.{vsl,psl}`), a third pipeline pair in `SDLWaterRenderer`, and `cWater::setTechnique` making the original's choice. **The port had read `WATER_EMPTY` as "the reflections-off look"; it is not.** `ca9aa43`'s `setTechnique` picks, on any PS2.0 card, `IsReflection() ? WATER_LINEAR_REFLECTION : WATER_REFLECTION` — `WATER_EMPTY` is the **no-PS2.0** path. Maelstrom ships `OPTION_REFLECTION = false`, so its water ran the flat `vPS11Color` fallback, whose only variation is an alpha crest term that saturates past ~16% depth: one uniform colour. Note the fragment shader has **no glint term** — the sun sliding over the crests is the sun *in the cubemap*, displaced by `cube.xy += (tex0.xy+tex1.xy)*0.3`; do not invent a specular for it. `uv_mirror` is `normalize(camPos - pos)` with `dir.z -= pos.z`, the eye vector mirrored about z=0 rather than a real surface normal — ported verbatim. At a low camera this direction is nearly horizontal, so the reflection is a horizon one, not a mirror of the zenith. Depends on #9, and was the bug that exposed #9's copy-ordering fault. |

## Features with no SDL path

| # | Feature | Where it died | What the original did |
|---|---------|---------------|-----------------------|
| 6 | **Post-effect stack, the remainder** (monochrome, under-water and the lens flare are ported — #6a/#6b above; the capture + fullscreen-pass infrastructure they needed now exists) | `VistaRender/postEffects.cpp` — the effects `PostEffectManager::createEffect` declines to create | **DOF** — reachable on the top graphics preset. Its depth source now exists — #12's scene-depth snapshot is exactly the full-screen float map `PostEffectDOF::redraw` sampled — so what is left is the effect itself (the blur + combine passes) and a decision on #13 below. Note `cVisGeneric::SetEnableDOF` still gates on `gb_RenderDevice3D->IsPS20()`, which is permanently false. **Bloom + the screen flash** — the manager masks bloom out of creation (`enabledEffects_ &= ~(1 << PE_BLOOM)`, there since the init commit), so the nuke flash was invisible on shipped D3D9 too; kept masked on purpose — porting it would invent behaviour retail never showed. **Mirage** — an inverted-logic bug in `PostEffectMirage::createTextures` kept it from ever drawing on D3D9, and it needs the mirage camera (#11). **Colour-dodge** — zero uses in shipped content. Check reachability before porting any of these. |
| 11 | **Mirage camera** | `cScene::AddMirageCamera` (deleted) | Heat-haze render target, composited over the frame. |
| 13 | **Water depth prepass** | `cWater::DrawToZBuffer` | Stamped the water surface into #12's float map so depth effects saw the water, not the terrain under it. **Dead at `ca9aa43` already**: its one call site (`cWater::Draw`, Water.cpp:278) is commented out, so retail D3D9 shipped soft particles and DOF fading against the bottom, not the surface. If DOF (#6) is ever ported, decide then whether to resurrect this (a depth-only water pass into #12's snapshot) or keep retail behaviour. |
| 14 | **Render-target debug viewers** | `Camera::DrawShadowDebug`, `TempDrawShadow` (deleted) | The `Option_ShowRenderTextureDBG` overlays that blit the shadow/reflection/float maps to the corner of the screen. Dev tooling, but genuinely useful — worth restoring early. |
| 26 | **Adaptive terrain LOD** — the tiled, per-tile-subdivided heightfield mesh | `Render/D3D/D3DRenderTilemap.cpp` (kept, inert); `SDLTileMapRenderer` draws **one uniform grid** instead | The D3D9 tilemap tessellated per tile against the camera, so the drawn ground tracked the heightfield closely **where the camera was**, and coarsened only into the distance. The SDL renderer builds a single static grid at one `step_`, which cannot do both — and that is not just a silhouette question. Everything that lies flat on the ground is positioned against the **true per-cell heightfield**, with almost no clearance: `CircleManager::drawSpline` puts the select ring at `getAltWhole + 1` (one world unit), and the flat plate at the base of a unit's own 3DX model — the one carrying its legion colour, and the emblem `SetSkinColor(unitColor(), unitSign())` composites in at `logoPosition_` — has none at all. Where the grid interpolates above the real surface, all of it loses the depth test. **This was live and shipped-visible**, not theoretical: the step was chosen to fit a **16-bit index buffer**, which on Maelstrom's 2048×4096 heightfield forced `step 16` — one vertex per 16 world units — and unit decals showed on some soldiers and not others, flickered as the camera moved, and left a sliver of colour at the feet. Mitigated 2026-08-15 (32-bit indices, a 600k-vertex budget in place of the index cap, and a slope-scaled depth bias on the terrain/lava/ice pipelines): 512 maps now draw at `step 1`, 1024 at 2, 2048×4096 at 4. That is accuracy bought uniformly, by memory; the original bought it where it mattered, by LOD. **Restore the LOD if the budget ever binds** — raising it further is linear in vertices and redundant in the distance. |
| 24 | **`cChaos`** — the seething void drawn under and around the map when a world's `Outside` is `ENVIRONMENT_CHAOS` | `Render/src/CChaos.cpp` — `cChaos::Draw` returns early | The dual-bump-EMBM disc mesh (`Chaos/chaos.{vsl,psl}`), plus the `sBumpTile::SetVertexZ` hole-sinking in the terrain. **No shipped P2 world uses `ENVIRONMENT_CHAOS`** — it is a Perimeter-1 leftover, reachable only from the editors. Check for a consumer before spending time here — but check it against **both** games' data: #9 was written off as consumerless on exactly this reasoning, and that was true of Perimeter 2 alone. Its `DrawSprite2` path (#18) was dead even at `ca9aa43`. |

## Device capabilities the above need first

| # | Capability | Where |
|---|------------|-------|
| 15 | **Dynamic vertex/quad buffers** — `GetBufferXYZD`, `GetQuadBufferXYZDT1`, … all return `nullptr` | `cSDLRenderDevice`. ~25 files called them. **Check what a feature really uses before believing it is blocked on this — the register has been wrong four times.** Grass draws from *static* `sPtr` buffers the device already implements; the field dome had its own dynamic VB and now goes through `SDLWorldQuadRenderer`'s triangle route; the lens flare's screen quads ride the UI batch and its glow the world-quad route (#6b); the logo splash's cells (#23) are a batch of screen-space quads its own renderer builds. What is genuinely left: `cLeaves`, and the colour-dodge's rotating quad if that effect is ever ported. |
| ~~16~~ | ~~**Colour-write masks**~~ — **done**, in `SDLWorldQuadRenderer` | `SetColorWriteMask(eColorWriteMask)`, folded into the pipeline key: SDL GPU only consults `color_write_mask` when the pipeline sets `enable_color_write_mask`, so "write everything" stays the zeroed default. It is a *render state*, not part of the material — set around a run of groups and restored, as `D3DRS_COLORWRITEENABLE` was. Only the two lightmap callers use it (#10). No other renderer has one, and nothing else has asked. |
| 17 | **3D debug primitives** — `DrawLine`, `DrawPoint`, `drawCircle`, `DrawBound`, `FlushPrimitive3D` | `cSDLRenderDevice`, all no-ops. Used by `cTileMap::DrawLines` and the debug overlays. |
| 18 | **`DrawSprite2` / `DrawSpriteScale` / `DrawSpriteSolid`** | `cSDLRenderDevice`, all no-ops. Only `CChaos` (#24) uses them — and only on a path that was already dead at `ca9aa43`. |
| 20 | **Occlusion queries** | `cOcclusionQuery` — stubbed (`Init()` false, `VisibleCount()` 0). SDL GPU exposes none, so a real port means depth readback or a counting pass. Neither caller waits on it any more: #6b replaced the lens flare's ring test with a CPU visibility check, and #22 reads occlusion per fragment from the depth test instead of per object from the query. Note the stub does not fail closed — `cOcclusionSilouette::IsVisible` starts `!occlusionQuery.IsInit() || ...`, so a stubbed query reports everything **visible**, which is why #22 could not be ported faithfully on top of it. |
| 21 | **Screenshots** (`SetScreenShot`), **gamma** (`SetGamma`) | `cSDLRenderDevice`, no-ops. |
| 22 | **Fullscreen mode** | `PlatformWindow::create` always makes a windowed window; `IsFullScreen()` returns false. `OPTION_FULL_SCREEN` is ignored. |

## The dead D3D9 source is kept on purpose — do not "tidy" it away

`Render/shader/shaders.cpp`, `Render/shader/ShaderStorage.cpp`, `Render/src/CChaos.cpp` and
`Render/src/RenderCubemap.cpp` still compile, and still contain their full D3D9
implementations. **This is deliberate**: they are the reference for the ports above, and
`Environment` / `SkyObject` still construct and configure them (the settings come out of
world data). Nothing in them runs.

`VistaRender/postEffects.cpp` and `Render/src/LensFlare.cpp` are now **partly live**: the
manager, monochrome, under-water and the flare run on SDL (#6a/#6b), while the unported
effect classes in postEffects.cpp (bloom, DOF, mirage, colour-dodge) remain D3D9 reference
bodies that `createEffect` declines to construct.

They are inert because `gb_RenderDevice3D` is permanently null and the draw entry points return
early. If you add a new entry point into any of them, guard it the same way — a bare
`gb_RenderDevice3D->` or a `static_cast<cD3DRender*>(gb_RenderDevice)` is a null dereference or
a bad cast now, not a Windows-only path.

Getting them out of the build means unpicking `Environment`'s `PostEffectManager` / `Flash` /
`cChaos` plumbing and `SkyObject`'s cubemap, while keeping the serialization intact so world
data still loads. Do that when the features are ported, not before.

## The `_WIN32` guards that are still there on purpose

Every `#ifdef _WIN32` in the rendering code is gone, and so is the one in the in-place
serializer (the engine is 64-bit on every platform now, and reconstructs the 32-bit cache
images by hand rather than relocating them — see `Util/Serialization/InPlaceArchive.h`).

What remains are genuine OS-API guards, and deleting them would be wrong:

- **`Render/3dx/Saver.h`** — Win32 `_open`/`_read` vs POSIX `open`/`read`.
- **`Render/src/ftrender.cpp`** — `NormalizePath` (backslash → POSIX path, plus case
  resolution on case-sensitive volumes) exists only off-Windows, and is not wanted on Windows.
- **`Render/src/FileImage.cpp`** — the Video-for-Windows AVI reader.
- **`Game/Runtime.cpp`** — `_beginthread` for the threaded mission load; elsewhere the load
  is synchronous. A threading port, not a rendering one.

## Not the renderer, but still Windows-only

These block a genuinely single-platform-behaviour build and are tracked here so they aren't
forgotten, but they are separate from the D3D9 retirement:

- **Audio** — DirectSound (`Sound/`); off-Windows is a silent stub (`SoundStub.cpp`).
- **Networking** — DirectPlay 8 (`Network/`); off-Windows is a no-op stub (`NetCenterStub.cpp`).
- **Joystick** — DirectInput 8 (`Util/joystick.cpp`).
- **Video** — Bink (`Game/PlayBink.cpp`) and Video-for-Windows AVI (`Render/src/FileImage.cpp`).
  The main-menu backdrop is a Bink video.
- **Editors** — `kdw`, `TriggerEditor`, `ShaderToH2`, and the MSVC-only tool projects
  (`SurMap5`, `ModelViewer`, `VistaEditor`, …), which still reference the deleted D3D9 backend.
